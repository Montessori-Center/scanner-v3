# /scanner/.env.example
# 1) Inherit global project variables (one level up)
[ -f ../.env ] && . ../.env

# 2) Scanner-local settings
LOG_LEVEL=info
OUTPUT_DIR=output
LOG_DIR=logs

# 3) Optional: override DB only if scanner must use a different DB
# DB_HOST=127.0.0.1
# DB_PORT=3305
# DB_NAME=eternal_max
# DB_USER=em_user
# DB_PASS=change_me
